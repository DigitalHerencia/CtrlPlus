/**
 * Prisma client singleton.
 * This stub will be replaced with a real PrismaClient + PrismaNeon adapter
 * when @prisma/client and @neondatabase/serverless are installed.
 */

// TODO: Replace with real Prisma setup:
// import { PrismaClient } from "@prisma/client";
// import { PrismaNeon } from "@prisma/adapter-neon";
// const adapter = new PrismaNeon({ connectionString: process.env.DATABASE_URL });
// export const prisma = new PrismaClient({ adapter });

export const prisma = {} as Record<string, unknown>;
