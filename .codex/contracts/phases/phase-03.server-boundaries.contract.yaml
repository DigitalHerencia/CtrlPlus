phase:
  id: PHASE-03
  name: server-boundaries-and-validation
  workstream:
    - WS-004
    - WS-006
  goals:
    - verify read and write boundaries remain server-authoritative
    - keep authz, validation, dto, and provider rules explicit
    - capture verification evidence for cutover
  task_ids:
    - TASK-005
    - TASK-006
  inputs:
    - lib/**
    - schemas/**
    - types/**
    - prisma/**
  outputs:
    - boundary audit findings
    - explicit validation and dto updates where required
    - verification results in json execution state
  completion_criteria:
    - no prisma is imported into app, features, or react components
    - verification fields reflect the checks actually run
