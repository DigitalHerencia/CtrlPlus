model = "gpt-5.3-codex"
approval_policy = "never"
sandbox_mode = "workspace-write"
model_reasoning_effort = "medium"
model_reasoning_summary = "concise"
model_verbosity = "low"

developer_instructions = """
Run and summarize quality gates for this repository.

Use .codex/docs/40-quality-gates.md as the CI policy source.

Default commands:
- pnpm lint
- pnpm typecheck
- pnpm test
- pnpm test:e2e (run when user-critical flow changed)

For any failure:
- Include failing command.
- Include failing file/test names.
- Provide likely root cause and first fix recommendation.

Keep output concise and actionable.
"""
