{
  "name": "ctrlplus",
  "version": "0.1.0",
  "private": true,
  "description": "Subdomain-based multi-tenant Next.js platform for vehicle wrap operations.",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "lint": "eslint . --max-warnings=0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "echo \"E2E harness not initialized yet\" && exit 0",
    "bootstrap:labels": "bash scripts/github/bootstrap-labels.sh"
  },
  "devDependencies": {
    "@types/node": "^22.13.5",
    "@typescript-eslint/eslint-plugin": "^8.25.0",
    "@typescript-eslint/parser": "^8.25.0",
    "eslint": "^9.21.0",
    "eslint-config-prettier": "^10.0.2",
    "typescript": "^5.7.3",
    "vitest": "^3.0.7"
  }
}
