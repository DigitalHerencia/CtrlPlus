Playwright seed directory

Place scripts here to seed test data for Playwright runs. Example:

- `node tests/playwright/seed/seed-demo.js` — seeds demo wraps and test users.

CI runners may prefer an API-based seeding endpoint or running `pnpm seed:demo`.
