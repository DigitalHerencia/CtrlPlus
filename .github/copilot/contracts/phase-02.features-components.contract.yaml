phase:
  id: PHASE-02
  name: features-components-normalization
  workstream:
    - WS-002
    - WS-003
  goals:
    - normalize page-feature entrypoints and client module naming
    - classify reusable ui into domain, shared, or primitive layers
    - remove boundary violations between features and components
  task_ids:
    - TASK-003
    - TASK-004
  inputs:
    - features/**
    - components/**
  outputs:
    - consistent feature entrypoints
    - cleaned component ownership
    - updated json execution state
  completion_criteria:
    - feature modules remain orchestration-only
    - components/ui contains primitives only
